CREATE STREAM playevents (SONGID BIGINT, DURATION BIGINT) WITH (KAFKA_TOPIC='play-events', VALUE_FORMAT='avro');
CREATE TABLE songfeed (ID BIGINT, ALBUM STRING, ARTIST STRING, NAME STRING, GENRE STRING) WITH (KAFKA_TOPIC='song-feed', VALUE_FORMAT='avro', KEY='ID');
